# 학생 우울 라벨 탐색: 성능보다 해석 가능성을 우선한 로지스틱 회귀

[English](PORTFOLIO.md) | [한국어](PORTFOLIO.ko.md)

## 프로젝트 개요

이 프로젝트는 학생 설문 데이터에서 우울 라벨과 함께 나타나는 생활·학업 요인을 탐색한다. 의료 진단 모델을 만드는 것이 아니라, 어떤 변수 조합이 설문 라벨과 통계적으로 연관되는지를 투명하게 확인하는 분석이다. 그래서 복잡한 블랙박스 모델보다 계수와 p-value를 함께 볼 수 있는 로지스틱 회귀를 선택했다.

## 데이터와 변수 선택

포함된 설문 데이터는 502행이다. 전처리 단계에서 타깃과 범주형·순서형 응답을 인코딩하고, 상관 탐색과 기존 노트북의 유의성 검토를 거쳐 식습관, 자살사고 이력, 학업 압박, 재정 스트레스 네 변수를 최종 경로에 남겼다. 변수 수를 의도적으로 제한한 이유는 작은 설문 표본에서 무분별한 피처 확장이 만드는 해석 혼선을 줄이기 위해서다.

## 분석과 검증

학습 스크립트는 seed 42의 80/20 분할로 401개 학습 행과 101개 holdout 행을 만들고, scikit-learn 로지스틱 회귀와 statsmodels Logit을 함께 실행한다. 전자는 예측 지표를, 후자는 계수와 p-value를 제공한다. 결과는 단순한 정확도 하나가 아니라 혼동행렬과 클래스별 precision·recall·F1까지 `results/model_summary.txt`에 남긴다.

보관된 재실행 결과는 정확도 0.9010, weighted F1 0.90이다. 혼동행렬은 `[[42, 6], [4, 49]]`로, 48개 음성 라벨 중 42개와 53개 양성 라벨 중 49개를 맞췄다. 네 변수의 Logit 계수는 모두 p<0.001로 기록됐지만, 이 값은 설문 표본 안의 연관성을 보여줄 뿐 원인이나 임상적 위험을 뜻하지 않는다.

## 이 프로젝트가 보여주는 역량

이 작업에서 중요한 것은 높은 수치를 의료 성능처럼 포장하지 않은 점이다. 작은 표본에서 어떤 모델이 맞췄는지와, 어떤 변수가 그 예측에 들어갔는지를 동시에 확인할 수 있게 만들었다. 상관 그림, 응답 분포, learning curve를 따로 생성한 것도 결과표만으로 놓치기 쉬운 데이터 맥락을 보기 위해서다.

## 한계와 책임 있는 사용

검증은 단일 무작위 분할이고 층화 반복 검증·보정·외부 검증·하위집단 분석이 없다. 설문 라벨의 수집 방식도 임상 판정을 대체하지 않는다. 따라서 이 결과는 교육용·탐색용 분석으로만 사용하며, 개인 진단이나 자동화된 개입에 연결하지 않는다.

## 근거

- 결과 요약: [`results/model_summary.txt`](results/model_summary.txt)
- 학습 코드: [`src/train.py`](src/train.py)
- 전처리·시각화 구조: [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)

## 왜 해석 가능한 모델을 기준으로 삼았나

설문형 데이터는 변수 하나의 의미가 민감하고, 표본도 수백 행 수준이라 모델의 복잡도를 무작정 높이는 것이 답이 되기 어렵다. 이 프로젝트는 식습관·자살사고 이력·학업 압박·재정 스트레스라는 네 변수를 최종 모델에 남기고, 같은 입력에 대해 scikit-learn의 예측 지표와 statsmodels의 계수·p-value를 나란히 확인했다. 즉 “맞혔는가”와 “무슨 변수 조합에서 그렇게 판단했는가”를 분리하지 않았다.

보관된 혼동행렬 `[[42, 6], [4, 49]]`은 이 분할에서 양성 53개 중 49개를, 음성 48개 중 42개를 맞혔다는 뜻이다. weighted F1 0.90은 두 라벨의 지원 수를 반영한 평균이라, 특정 소수 집단에 대한 공정성이나 임상 민감도를 대신하지 않는다. 모든 계수가 p<0.001로 기록된 것도 이 표본과 이 변수 선택 안에서의 통계적 연관성일 뿐, 특정 요인이 우울을 일으켰다는 인과 결론은 아니다.

프로젝트는 학습 곡선, 응답 분포, 상관 탐색을 따로 남겨 숫자 하나만 보고 결론 내리는 일을 피했다. 앞으로는 반복 층화 검증, 외부 표본, 문항·문화권별 측정 동등성, 임상 전문가 검토가 필요하다. 그 전까지는 수업·연구 맥락에서 설문 라벨의 패턴을 탐색하는 도구로만 해석한다.
